import React from 'react';
import { Board } from './Board';

export const Chess = () => (
    <div className="chess">
        <h1 className="header-primary">Collatty chess</h1>
        <Board></Board>
    </div>
);
